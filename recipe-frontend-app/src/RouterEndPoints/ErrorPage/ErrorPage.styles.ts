import styled from "styled-components";

const Wrapper = styled.div`
  padding: 20px;
`;

const Title = styled.h2``;

const Information = styled.div`
  padding: 5px;
`;

const styles = { Title, Information, Wrapper };

export default styles;
