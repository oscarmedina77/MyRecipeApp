import styled from "styled-components";

const RecipesWrapper = styled.div`
  display: flex;
  gap: 20px;
  justify-content: center;
  flex-wrap: wrap;
`;

const Title = styled.h2`
  padding: 10px;
`;

const styles = { RecipesWrapper, Title };

export default styles;
